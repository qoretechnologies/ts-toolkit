# Credentials of the live Qorus instance used by the integration test suites.
# Leave this file alone and set nothing to run only the tests that need no server:
# the integration suites then report as skipped rather than failing.
TESTPASS=
TESTUSER=
ENDPOINT=